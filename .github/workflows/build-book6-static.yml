name: Build Book VI static corpus

on:
  push:
    branches: [master]
    paths:
      - 'apps/studio/scripts/import-cic/generate-book6-static.mjs'
      - '.github/workflows/build-book6-static.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Generate static Book VI corpus
        run: node apps/studio/scripts/import-cic/generate-book6-static.mjs
      - name: Verify generated corpus
        run: |
          test -f apps/studio/scripts/import-cic/data/canons.1311-1399.static.ts
          grep -q 'canons1311to1399' apps/studio/scripts/import-cic/data/canons.ts
      - name: Commit generated corpus
        run: |
          git config user.name 'fonte-iuris-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add apps/studio/scripts/import-cic/data/canons.1311-1399.static.ts apps/studio/scripts/import-cic/data/canons.ts
          if git diff --cached --quiet; then
            echo 'No generated changes'
          else
            git commit -m 'data: materialize static CIC Book VI corpus'
            git pull --rebase origin master
            git push origin HEAD:master
          fi
